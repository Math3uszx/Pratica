#include <stdio.h>
#include <stdlib.h>

int main()
{
    float nota;
    float notas[3];
    float media = 0;
    int i;
    float soma = 0;
    
    for(i = 0;i < 3;i++){
        
        printf("Digite a %dº nota:",i + 1);
        scanf("%f",&notas[i]);
        
        soma += notas[i];
    }
        media = soma / i;
        
        for(i = 0;i < 3;i++){
        
        printf("nota: %.1f \n", notas[i]);
        }
    printf("media: %.1f \n",media);

    return 0;
}
